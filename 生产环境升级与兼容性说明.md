# 生产环境升级与兼容性说明

> 适用范围：本项目已在生产环境正式使用。本说明用于后续开发、部署和升级时保护既有业务功能与生产数据。

## 1. 基本原则

- 本次“未救助明细台账”按新增模块处理，不改写既有参保、医疗救助、统计汇总、优抚结算等业务主表。
- 允许复用平台底座：登录、用户、角色、权限、任务中心、导入导出、操作日志。
- 数据库升级必须采用增量迁移，不允许使用 `migrate:fresh`、`migrate:reset`、`migrate:refresh` 等会清空或回滚生产结构的命令。
- 生产环境上线前必须先备份数据库，并记录当前代码版本、数据库版本和迁移执行结果。
- 涉及菜单权限初始化的命令要谨慎执行。当前 `init:menu-permissions` 会重建权限数据，执行后需要重新给角色分配权限。

## 2. 本次新增内容

新增表：

- `towns`
- `operation_logs`
- `unrescued_records`
- `unrescued_disease_configs`
- `unrescued_wash_configs`
- `unrescued_wash_logs`
- `unrescued_supplement_records`

扩展旧表：

- `users.town_id`
- `permissions.status`

影响说明：

- `users` 只新增可空字段 `town_id`，旧用户默认为空，表示区级/全局账号，不影响原有登录和权限逻辑。
- `permissions` 新增 `status`，默认 1，表示既有权限默认启用；用于兼容现有菜单/权限过滤逻辑。
- 新模块业务数据写入 `unrescued_*` 表，不与旧业务主表混写。
- 镇街数据隔离只在后续未救助模块业务查询中使用，不改变旧模块的数据查询范围。

## 3. 迁移目录说明

当前 Hyperf 版本的默认迁移目录是根目录 `migrations/`。

项目历史上部分迁移文件存放在 `database/migrations/`，但直接执行：

```bash
php bin/hyperf.php migrate
```

不会扫描 `database/migrations/`，因此会出现文件存在但提示 `Nothing to migrate.` 的情况。

本次已在根目录新增 `migrations/`，并放入本次未救助模块的 8 个生产增量迁移文件。后续需要通过默认命令执行的生产迁移，应继续放在根目录 `migrations/`。

`database/migrations/` 可作为历史参考目录，除非明确指定 `--path`，否则不会被默认迁移命令执行。

## 4. 生产升级建议流程

1. 确认运行环境满足 Composer 要求：

```bash
php -v
composer check-platform-reqs
```

2. 备份数据库：

```bash
mysqldump -h DB_HOST -u DB_USER -p DB_NAME > backup_$(date +%Y%m%d%H%M%S).sql
```

3. 拉取或发布新代码。

4. 检查待执行迁移：

```bash
php bin/hyperf.php migrate:status
```

5. 执行增量迁移：

```bash
php bin/hyperf.php migrate --force
```

6. 核对表结构：

```sql
show tables like 'towns';
show tables like 'operation_logs';
show tables like 'unrescued_%';
show columns from users like 'town_id';
show columns from permissions like 'status';
select migration from migrations where migration like '2026_05_24_100%';
```

7. 初始化或补充菜单权限。

当前命令会重建权限数据：

```bash
php bin/hyperf.php init:menu-permissions
```

执行后需要在后台重新给角色分配权限。若生产环境已有复杂角色配置，建议先导出 `roles`、`permissions`、`role_permissions`、`role_user` 数据，再执行命令并人工核对。

8. 重启后端服务和队列进程。

9. 登录后台验收：

- 原有模块可正常访问。
- 原有用户可正常登录。
- 镇街管理可新增、编辑、删除。
- 用户管理可选择或清空镇街。
- 重大疾病编码可新增、编辑、删除、在导入弹窗内下载模板、提交导入任务。
- 镇街管理可在导入弹窗内下载模板并提交 CSV 导入任务。

## 5. 回滚原则

代码回滚：

- 优先回滚到上一版本代码。
- 保留已新增表不影响旧业务运行。

数据库回滚：

- 生产环境不建议直接执行 `migrate:rollback`，除非确认本批迁移没有被业务使用。
- 若必须回滚，先停服务、备份当前库，再按本次迁移的反向顺序回滚。
- 若已产生新模块业务数据，应优先保留新增表，避免误删用户导入成果。

## 6. 后续开发要求

- 新功能涉及生产升级时，必须同步更新本文档和 `TODO_未救助台账开发交接文档.md`。
- 新迁移默认放入根目录 `migrations/`，保证 `php bin/hyperf.php migrate` 可直接发现。
- 如需保留 `database/migrations/` 里的副本，只作为开发参考，不作为生产默认执行路径。
- 导入模板按现有前端习惯放在 `tools-react-yf/public/assets/templates`，模板下载入口放在导入弹窗内部，列表页不单独放模板下载按钮。
- 对旧表只允许做兼容性新增字段或新增索引，不允许改名、删除字段、修改旧字段语义。
- 所有重要操作需要写操作日志，涉及删除、下放、导出、清洗等动作需要二次确认。
