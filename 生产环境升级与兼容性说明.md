# 生产环境升级与兼容性说明

更新时间：2026-05-31

## 本次业务范围

本次新增“未救助台账”相关能力，并补齐镇街管理、任务中心、导入导出、清洗规则、动态筛选与权限节点的配套逻辑。

主要入口：

- 用户管理 / 镇街管理：维护镇街基础数据，支持导入。
- 用户管理 / 账户管理：用户可绑定镇街；绑定后按镇街账号处理。
- 用户管理 / 操作记录：查看全局操作日志，支持按模块、操作、账号、状态、时间和关键词筛选。
- 用户管理 / 业务筛选项：维护通用业务筛选候选项，支持未救助清洗规则等业务复用。
- 未救助台账 / 未救助明细：导入附件1、导入附件2、执行清洗、下放镇街、镇街接收、通知、账户回填、报销标记、导出。
- 未救助台账 / 重大疾病编码：维护重大疾病编码字典，支持导入。

## 上线执行顺序

1. 备份生产库。
2. 执行本次 migrations。
3. 执行菜单权限初始化命令：`php bin/hyperf.php init:menu-permissions`。
4. 确认角色授权，给相关角色勾选“镇街管理”“未救助台账”“未救助明细”“重大疾病编码”等菜单及操作权限。
5. 重启 Hyperf 服务和队列消费进程。
6. 前端重新打包并发布：`npm run build:prod`。
7. 清浏览器缓存或重新登录，避免旧权限缓存影响菜单展示。

## 服务端 Migrations

本次业务保留的 migrations：

- `2026_05_24_100001_create_towns_table.php`：镇街表。
- `2026_05_24_100002_add_town_id_to_users_table.php`：用户绑定镇街。
- `2026_05_24_100003_create_operation_logs_table.php`：业务操作日志。
- `2026_05_24_100004_create_unrescued_records_table.php`：未救助明细主表。
- `2026_05_24_100005_create_unrescued_disease_configs_table.php`：重大疾病编码字典。
- `2026_05_24_100006_create_unrescued_wash_configs_table.php`：清洗规则配置，已包含 `rule_type`。
- `2026_05_24_100007_create_unrescued_wash_logs_table.php`：清洗执行日志。
- `2026_05_24_100008_create_unrescued_supplement_records_table.php`：应补应退排查记录。
- `2026_05_24_100009_add_status_to_permissions_table.php`：权限节点启停状态。
- `2026_05_24_100010_seed_unrescued_permissions.php`：未救助权限节点兜底写入。
- `2026_05_24_100011_create_business_filter_options_table.php`：业务筛选项与清洗规则自定义选项。

说明：

- 新业务上线前如果生产还没有数据，可直接把建表 migration 调整到最终结构，避免再追加一堆“修补字段”的 migration。
- 已上线且有数据的功能，不允许直接改历史 migration，应新增兼容 migration。
- `database/migrations` 和 `migrations` 目录要保持一致，避免本地和容器执行路径不一致。
- 大数据量筛选字段要在建表时同步考虑索引，如清算期、镇街、状态、剔除状态、报销状态、医疗类别、机构名称、身份类别等。

## 权限节点初始化

`app/Command/InitMenuPermissionsCommand.php` 已按当前库里的节点重新整理：

- 不再指定固定 ID。
- 以 `name` 作为唯一业务标识做非破坏性 upsert。
- 已存在节点保留原 ID，因此不会破坏 `role_permissions` 关联。
- 父子关系通过 `parent` 名称回填 `parent_id`。
- 执行完成后会清理 `user:cache:*`，避免用户权限缓存导致菜单不刷新。

当前节点重点：

- `镇街管理` 挂在 `用户管理` 下。
- `操作记录`、`业务筛选项` 挂在 `用户管理` 下。
- `未救助台账` 为顶级菜单，子菜单为 `未救助明细`、`重大疾病编码`。
- `类别额度配置` 已按生产已有节点纳入初始化，但不再硬编码 ID。
- `联网结算` 也纳入当前初始化定义。

如果菜单不显示，优先检查：

- `permissions` 表里菜单节点 `status = 1`。
- 当前角色是否勾选了菜单节点或其操作节点。
- `/api/user/info` 返回的 `permissions` 是否包含对应节点。
- `/api/permissions/user/menus` 是否返回对应菜单。
- 用户是否仍命中旧 Redis 缓存，必要时执行初始化命令或清理 `user:cache:*` 后重新登录。

## 前端路由与菜单注意事项

前端目前是静态路由 + 后端动态菜单一起使用，容易踩几个坑：

- `.umirc.ts` 必须存在真实页面路由，否则动态菜单展示了也可能跳 404。
- `src/app.ts` 的菜单转换必须过滤 `type !== 'menu'` 的操作节点，否则操作权限会被误当成子路由。
- `src/access.ts` 的模块权限判断需要同时支持“菜单节点名”和“操作节点名前缀”，例如有 `镇街管理` 或 `镇街管理:查看` 都应允许进入模块。
- 生产接口不要手写相对 `fetch('/api/xxx')`，统一使用项目 request 或 `${config.apiBaseUrl}`，否则打包后可能出现 `/api` 前缀、反代路径不一致导致 404。
- 前端打包后如菜单异常，先用 `npm run dev:prod` 对同一套生产接口复现，再确认 `env.ts`、接口 baseURL、后端菜单数据和角色权限。

本次已处理过的典型问题：

- `镇街导入` 使用原生 `fetch('/api/towns/import')` 时生产环境 404，改为统一 request 后走同一套 baseURL。
- 动态菜单里 operation children 会影响叶子菜单展示，已在菜单转换里只保留 `type = menu`。
- 用户信息接口需要取最新角色权限和镇街信息，不能长期依赖旧缓存。

## 未救助台账页面交互标准

列表字段较多，本次按“默认可用 + 用户可配置”处理：

- 默认固定关键列：清算期、姓名、身份证号等靠左，操作类或必要列可靠右。
- 列设置支持显示/隐藏、左固定、右固定、不固定。
- 左右固定互斥，点击同侧固定图标可取消固定。
- 列设置支持拖拽排序，列表列顺序随配置变化。
- 配置写入 localStorage，刷新后保留。
- 镇街账号隐藏不应看到的字段：剔除、命中状态、报销等。

筛选交互：

- 清算期手动切换后写入缓存，刷新仍使用上一次选择。
- 切换清算期后必须重新请求列表、统计、清洗选项。
- “重置/清空筛选”不清空清算期。
- 镇街账号只保留基础筛选项，并展示当前所属镇街，不允许编辑。
- 管理端筛选较多时使用收起/展开，避免顶部过于臃肿。

## 未救助业务规则概览

导入附件1：

- 写入 `unrescued_records`。
- 以批量读取、批量插入/更新方式处理，减少逐行查询。
- 金额字段必须按数值入库；排序时确保字段是 decimal/numeric，不要把金额当字符串排序。

导入附件2：

- 用于匹配救助对象名单和镇街。
- 镇街匹配使用标准化名称和别名映射，避免逐行查库。
- 未匹配到镇街的数据可以保留在台账里，但不可下放给镇街。

清洗规则：

- 清洗规则配置存入 `unrescued_wash_configs`，规则类型通过 `rule_type` 区分。
- 医疗类别保留、身份类别剔除支持用户自定义输入。
- 用户自定义选项会同步写入 `business_filter_options`，后续继续作为候选项使用。
- 执行清洗前需要二次确认。
- 清洗中按钮不可重复点击，并通过任务状态展示进度。
- 页面刷新或重新进入后，应继续通过接口读取当前执行状态。

下放与镇街：

- 管理端按清算期和镇街下放未剔除且可下放的数据。
- 镇街账号只能看到自己镇街的数据。
- 镇街确认接收后，状态进入镇街处理流程。

导出：

- 导出走异步任务，完成后在任务中心下载。
- 排查明细首列包含清算期。
- 未报销台账、通知名单应导出未剔除数据；即使部分字段为空，也应保留行给人工处理。
- 镇街账号导出不应包含剔除、命中状态、报销等管理字段。
- 备注列要优先取业务备注字段，不能只读模板中的空列。

重大疾病编码：

- 当前主要是字典维护和导入能力，落表 `unrescued_disease_configs`。
- 目前未直接接入清洗、附件4过滤或导出逻辑。
- 如果后续要按文档实现“门诊慢特病/重大疾病编码过滤”，应明确规则后接入清洗或附件4导出查询。

## 队列与任务中心标准

导入、导出、清洗这类耗时任务统一走 `TaskService::dispatchTask()`：

- 创建 `tasks` 记录，初始进度 `0`，状态 `pending`。
- 投递 Hyperf async queue。
- 使用 Redis lockKey 防重复提交。
- Job 开始时更新为 running。
- Job 按批次更新 progress 和 message。
- Job 成功时写入 result/file_path，状态 completed，progress 100。
- Job 失败时记录错误信息，状态 failed，并释放锁。
- 任务完成或失败后必须释放 Redis 锁。

日志标准：

- 接口提交任务时记录操作日志。
- Job 开始、进度、完成、失败都写 runtime log。
- 日志里要包含 uuid、用户、清算期、导入/导出类型、处理总数、成功数、失败数、耗时。
- 大批量任务每批记录一次进度，不要逐行刷日志。

前端标准：

- 提交任务成功后提示“请在任务中心查看进度”。
- 对清洗这类当前页面强相关任务，页面内也要轮询状态。
- 当进度变化时同步刷新统计卡片，让用户能肉眼看到数据变化。
- 已完成任务不应继续显示“执行中”，重新进入页面要能看到最新完成状态或任务中心记录。

## 接口路由标准

本次涉及的主要接口：

- `/api/towns`：镇街列表、创建、更新、删除。
- `/api/towns/options`：镇街选项。
- `/api/towns/import`：镇街导入。
- `/api/operation-logs`：操作记录列表。
- `/api/business-filter-options`：业务筛选项列表、创建、更新、删除。
- `/api/unrescued/records`：未救助列表、详情。
- `/api/unrescued/records/import-attachment1`：附件1导入。
- `/api/unrescued/records/import-attachment2`：附件2导入。
- `/api/unrescued/records/wash-config`：清洗规则获取/保存。
- `/api/unrescued/records/wash-options`：清洗规则候选项。
- `/api/unrescued/records/wash/execute`：执行清洗。
- `/api/unrescued/records/wash/status`：清洗进度。
- `/api/unrescued/records/distribute`：下放镇街。
- `/api/unrescued/records/receive`：镇街接收。
- `/api/unrescued/records/notify`、`/unnotify`：通知状态。
- `/api/unrescued/records/accounts`：账户回填。
- `/api/unrescued/records/reimbursement`：报销标记。
- `/api/unrescued/records/export`：导出。
- `/api/unrescued/disease-configs`：重大疾病编码列表、创建、更新、删除。
- `/api/unrescued/disease-configs/import`：重大疾病编码导入。

新增接口时需要同步检查：

- `config/routes.php` 是否注册。
- 控制器注解和手动路由是否一致。
- 前端 service 是否统一走 request。
- 生产反向代理是否只代理 `/api`，不要写错成双 `/api/api`。
- 权限名称是否已加入 `access.ts` 和节点初始化。

## 数据权限与镇街账号

镇街账号判断标准：用户 `town_id > 0`。

镇街账号规则：

- 列表查询自动追加当前用户 `town_id`。
- 筛选栏展示当前镇街，不允许切换。
- 不允许导入附件1、附件2。
- 不允许执行清洗和下放。
- 不展示剔除、命中状态、报销等管理字段。
- 只允许处理通知、账户回填等镇街侧流程需要的操作。

管理端规则：

- 可查看全量数据。
- 可按镇街筛选。
- 未匹配镇街的数据可以保留和导出，但不能下放。

## 开发其他业务时复用的标准

后端：

- 新业务先设计最终表结构，建表 migration 一次到位。
- 有数据后的字段调整才新增 patch migration。
- 耗时任务统一走任务中心，不在 HTTP 请求中同步处理大文件。
- 任务必须有锁、进度、日志、失败原因和可下载结果。
- 导入大文件优先 CSV 流式读取、批量入库、批量匹配，避免逐行查库。
- 业务配置型下拉项统一沉淀到业务 options 表，支持用户自定义并复用。
- 菜单权限通过初始化命令按 name upsert，不依赖固定 ID。

前端：

- 页面接口统一放到 service 层，使用项目 request。
- 菜单展示、静态路由、access 权限三者必须同时补齐。
- 大字段列表默认提供列显示配置、列固定、列顺序持久化。
- 角色不同的页面要做字段、筛选、操作按钮差异化。
- 需要长期任务进度的页面，除任务中心外要提供页面内状态反馈。
- 本地 `dev` 正常不代表生产包正常，涉及菜单/权限/路由时必须用 `dev:prod` 或生产包验证。

## 上线后核验清单

- `php bin/hyperf.php migrate` 无报错。
- `php bin/hyperf.php init:menu-permissions` 执行成功，无新增异常节点。
- 管理员能看到“镇街管理”“未救助台账”“未救助明细”“重大疾病编码”。
- 普通角色授权后能看到对应菜单。
- 镇街账号只能看到所属镇街数据，且隐藏管理字段。
- 镇街导入接口 `/api/towns/import` 返回任务 uuid。
- 附件1、附件2、重大疾病编码导入均进入任务中心。
- 执行清洗有二次确认、进度展示、不可重复点击。
- 清洗完成后列表、统计、任务中心状态一致。
- 三类导出能在任务中心下载，备注列和清算期列正确。
- `runtime/logs/hyperf.log` 能看到导入、导出、清洗的开始、进度、结果日志。
