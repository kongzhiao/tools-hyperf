# 系统用户管理业务变更增量

## 2026-07-12

- 新增：建立系统用户管理后端文档基线。
- 确认：后台账号、JWT、RBAC和镇街范围的当前实现。
- 确认：用户 `id=1` 为超级管理员，列表和可分配角色均排除对应保护记录。
- 确认：用户密码为单向哈希，编辑空密码保持不变。
- 已知限制：用户无启停与软删除；删除不可恢复。
- 已知限制：用户管理路由组没有统一挂载细粒度权限中间件；query Token 仍为存量兼容。
- 已知限制：部分认证错误可能暴露底层异常文案，本次只记录不修改代码。

