# 未救助台账业务变更增量

## 2026-08-03

- 调整：系统先按进入报销金额确定基础状态，仅金额大于300元的记录参与“门诊重大疾病匹配”。
- 调整：重大疾病命中不再覆盖金额分档状态；命中后仍按当前台账配置保留或剔除，并跳过后续普通规则。
- 修复：避免零金额、负金额和300元以内记录因命中重大疾病编码被改为拟通知2。
- 同步：未救助明细与应补应退明细使用相同金额前置条件，重新执行筛查可刷新未进入通知流程的旧结果。

## 2026-07-12

- 新增：未救助明细、应补应退明细各自独立增加第一优先“门诊重大疾病匹配”筛查规则。
- 规则：默认医疗类别为“门诊慢特病、造口袋门诊”，指定病种编码为 `M00500`，并自动精确匹配重大疾病编码库的启用记录。
- 结果：命中记录统一标记拟通知2；默认保留且跳过后续规则，配置为剔除时仍为拟通知2并跳过后续规则。
- 调整：用户可见“清洗”统一改为“筛查”，兼容性接口、权限、数据库字段和内部类名保持不变。
- 调整：帮助页内容、泳道流程图、前后端功能文档同步更新。
- 新增：建立未救助台账后端文档基线。
- 确认：附件1、附件4、两个独立附件2匹配、筛查规则和金额分档与当前代码一致。
- 确认：通知流程以 `unrescued_notice_records` 和 `/api/unrescued/notice-records` 为准。
- 确认：下放、接收、通知、反馈、备注、报销和镇街范围与 2026-07-12 帮助页一致。
- 确认：CSV编码、任务锁、进度和失败原因由公共服务处理。
- 已知限制：多个应用匹配键没有数据库唯一约束。
- 已知限制：敏感反馈字段为普通字符串存储；本次仅记录，不调整加密或脱敏。
- 已知限制：`unrescued_supplement_records` 仍存在但不属于当前三个主节点，后续使用前需重新确认。
