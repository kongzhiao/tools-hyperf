# 联网结算业务变更增量

## 2026-08-23

- 调整：“联网结算”改为父菜单，“联网结算明细”作为第一个子菜单。
- 调整：类别额度配置从停用的“业务配置”迁入“联网结算”，排序位于联网结算明细之后。
- 调整：文档按实际功能入口归档，不再保留独立“业务配置”文档。
- 明确：类别额度在明细导入和重新计算时使用，配置修改不会自动更新历史明细。
- 明确：旧 `settlement_configs` 接口不参与当前联网结算，当前业务以 `yf_category_quotas` 为唯一额度配置来源。
