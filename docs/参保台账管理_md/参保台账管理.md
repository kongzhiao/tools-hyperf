# 参保台账管理（数据按年份生效）

一、按年份配置资助参保优先身份、救助参保优先身份两种配置，导入-附件1《资助身份优先级配置》；数据表名:enroll_configs 参保配置表

    1、资助参保身份配置：导入 导入-附件1《资助参保身份优先级配置》 或 克隆历史配置；
        - 序号：（优先级，值越小优先级越高,可用于排序）
        - 资助参保身份
        - 资助档次
        - 资助标准
        - 个人实缴金额
        - 资助代缴金额

    2、医疗救助身份配置：导入 导入-附件2《医疗救助身份优先级配置》或 克隆历史配置；
        - 序号：（优先级，值越小优先级越高,可用于排序）
        - 医疗救助身份
        - 包含参保身份：（如：“在乡重点优抚对象（不含1-6级残疾军人）” 还包含“烈士遗属”、“因公牺牲军人遗属”、“病故军人遗属”）

    说明：
        - 可导入模板、也可基于选择某年的配置进行克隆；
        - 增加自增主键，记录可增删改查；
        - 包含参保身份：交互可用户自定义添加、编辑(输入后可回车添加)；

二、按年份配置 身份对应明细配置，导入 导入-附件7《身份对应明细配置》 或 克隆历史配置；
        - 特殊人员身份
        - 实缴金额

三、按月导入 导入-附件3《资助参保对象全量明细》;数据表名：enroll_ledger 参保台账表；与上月的数据比对；
    
    1、按年度根据身份证号码作为匹配条件，当用户不存在时即创建，存在则更新（按每人/年为维度一条记录），列字段：
        - 姓名
        - 身份证号
        - 镇（街）
        - 村（居）
        - 纳入资助时间：根据导入明细的“纳入资助时间”，第一次导入时的时间存储如（2026-03）,如果该字段已有时间则忽略以已有的为准；
        
    2、以导入的数据计算并写入明细表的对应字段：
        - 医疗救助身份记录：根据导入的“医疗救助身份”匹配医疗救助身份配置，累计去重后存入，可以有多个身份；建议存储为json数组，key的值为配置表的优先级，value为资助参保身份名称，并根据key升序后取出优先级最高的赋值给医疗救助身份；
        - 医疗救助身份：根据医疗救助身份json计算优先级最高的；
        - 资助参保身份记录：根据导入的“医疗救助身份”匹配资助参保身份配置，累计去重后存入，可以有多个身份；建议存储为json数组，key的值为配置表的优先级，value为资助参保身份名称，并根据key升序后取出优先级最高的赋值给资助参保身份；
        - 资助参保身份：根据医疗救助身份记录json计算优先级最高的；
        - 身份变更情况：新增、变更、取消 ？TODO::是不是 如果 本月导入数据 在本年里 无数据时为新增；如果本年已有数据且资助参保身份（优先身份）有变更时则为变更；如果 本年已有该人员数据 且 本月导入数据里不包含该人员则为取消？
        - 身份取消时间：？TODO：：本年已有该人员数据 且 本月导入数据里不包含该人员，则计入本月？

    说明：
        导入的明细记录里，一个人可能存在多条记录也可以存在多个身份；

四、导入 导入-附件4《困难人员参保核实》；

    1、以导入的数据计算并写入明细表的对应字段：
        - 居民医保缴费金额：对应导入的 个人缴费金额 列；
        - 资助地或参保地（区外备注）：核查对象参加区外市内或市外医保的情况？TODO：：


五、导入 导入-附件5《税务请款明细》；

    1、以导入的数据计算并写入明细表的对应字段：
        - 资助金额：对应导入的 代缴金额 列；
        - 获得资助身份类别：对应导入的 代缴金额 列,没有或0就显示空白，否则 ？TODO：：
        - 税务第一批请款：后续导入附件5《税务请款明细》“代缴金额”列 ？ TODO：：

    2、基于以上导入，再次计算以下列：
        - 参保类别：居民医保缴费金额、资助金额、资助参保身份，匹配 资助参保身份配置 的 个人实缴金额、资助代缴金额、资助参保身份 列，匹配 资助档次的值，若未匹配到则存为 需核实；
        - 是否参保：如果 参保类别 是“居民一档”||“居民二档”||“大学生一档”||“大学生二档”||“需核实”则显示 是，其他则为 否；
        - 未参保原因：如果 是否参保 为 是，则存 无，否则存 空字符串；
        - 是否符合资助：匹配 身份对应明细配置，实缴金额=居民医保缴费金额 & 特殊人员身份=获得资助身份类别 & 纳入资助时间、缴费时间 均为同一个月，生成“是”或“否”。？TODO：：缴费时间 是怎么来的？
        - 是否获得资助： 如果 是否符合资助 为 是 且 资助金额 > 0 则为 是，反之为 否；？ TODO：：需确认是否是这样？
        - 资助方式：是否获得资助 如果 是，则为 系统资助，反之为 事后资助；
        - 缴费时间：由我们后续自行导入（可能每月都会导入一次） ？ TODO：：导入表格是怎样的？如果没有导入的话以上几点的结果都没办法计算。


六、导入 导入-附件6《死亡人员名单》.md

    1、以导入的数据计算并写入明细表的对应字段：
        - 备注（卫健委死亡时间）：死亡时间 列,（可更改）


七、导出 导出-附件1《资助参保对象-汇总名单》

    1、完成步骤三之后，即可导出
    2、导出列字段说明：
        - 姓名
        - 身份证号码
        - 镇（街）
        - 村（居）
        - 资助参保身份：根据导入的
        - 医疗救助身份
        - 特困人员：根据“资助参保身份记录”字段，匹配当前类型存在则“√”，不存在留空
        - 城乡孤儿：根据“资助参保身份记录”字段，匹配当前类型存在则“√”，不存在留空
        - 事实无人抚养儿童：根据“资助参保身份记录”字段，匹配当前类型存在则“√”，不存在留空
        - 低保对象：根据“资助参保身份记录”字段，匹配当前类型存在则“√”，不存在留空
        - 低保边缘户：根据“资助参保身份记录”字段，匹配当前类型存在则“√”，不存在留空
        - 在乡重点优抚对象（不含1-6级残疾军人）：根据“资助参保身份记录”字段，匹配当前类型存在则“√”，不存在留空
        - 老复员军人：根据“资助参保身份记录”字段，匹配当前类型存在则“√”，不存在留空
        - 烈士遗属 ：根据“资助参保身份记录”字段，匹配当前类型存在则“√”，不存在留空
        ...（根据年份配置资助参保优先身份里的补齐列）

八、导出：导出-附件2《资助参保对象-对比结果》

    1、完成步骤三之后，即可导出
    2、导出列字段说明：
        - 增量形式
        - 姓名
        - 身份证号码
        - 医疗救助身份
        - 纳入资助时间
        - 身份取消时间

九、导出：导出-附件3《特殊对象资助参保台账》
    1、完成步骤六之后，即可导出；
    2、导出列字段说明：
        - 序号 
        - 纳入资助时间 
        - 身份取消时间 
        - 身份变更情况 
        - 镇（街） 
        - 村（居） 
        - 姓名 
        - 身份证号码 
        - 医疗救助身份 
        - 资助参保身份 
        - 是否参保 
        - 参保类别 
        - 居民医保缴费金额 
        - 缴费时间 
        - 是否符合资助 
        - 是否获得资助 
        - 获得资助身份类别 
        - 资助方式 
        - 税务第一批请款税务清款情况 
        - 资助金额 
        - 资助地或参保地（区外备注） 
        - 备注（卫健委死亡时间） 
        - 未参保原因
        
    3、该台账导出后的文件，可调整表格内容后，再次导入至系统并更新数据；

