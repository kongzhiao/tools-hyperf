
现在我们回到未救助明细台账管理，
我重新整理了该功能的业务调整，以下是调整的具体内容，先阅读现有业务再与我确认新的设计思路：

# 未救助明细

## 未救助明细 功能需要做调整： 

1、未救助明细 的调整：
    - 移除掉  下放镇街 > 镇街接收、通知和账户回填 > 报销标记 这几个流程；
    - 导出 也 不需要下拉选择了，而是直接导出 未救助明细表;
    - 未匹配 的列表页面可筛选，导出时也需要导出该标记；
    - 导入附件1未救助明细 时，进入报销金额 = 医保政策范围费用 - 统筹报销金额 - 大额报销 - 大病报销
    - 导入附件1未救助明细 时，进入报销金额 现在是 进入报销金额 <= 0 无救助金额 ，0 < 进入报销金额 <= 300	不通知，进入报销金额 > 300	拟通知；需改为：进入报销金额 <= 0 无救助金额 ，0 < 进入报销金额 <= 300	拟通知1，进入报销金额 > 300	拟通知2；
    - 现有流程：选择清算期 > 导入 附件1未救助明细 > 导入附件2救助对象名单 > 配置并执行清洗 > 下放镇街 > 镇街接收、通知和账户回填 > 报销标记，需调整为：选择清算期 > 导入附件1未救助明细 > 导入附件2救助对象名单 > 配置并执行清洗
    - 增加 病种编码/病种名称 筛选；

2、增加 应退应补明细 节点页面，功能与 未救助明细 几乎一样；
    - 流程：选择清算期 > 导入 附件4应补应退明细.md > 导入附件2救助对象名单（格式与未救助明细一样至于是不是同样的文件不一定，根据提供导入的数据匹配就行） > 配置并执行清洗
    - 导入附件4应补应退明细时，进入报销金额 =医保政策范围费用-统筹报销金额-大额报销-大病报销-医疗救助金额-渝快保报销金额
    - 清洗规则增加：
        - 导入附件4应补应退明细时，总费用 列 如果有负数的，则查看该人员是否有同样 金额 的正数记录；清洗时需要把这两条正负记录剔除掉（相加为0的），通常可能会有3条记录（即正负相加为0的剔除后就保留正数或负数中未被抵消的记录）；
        - 医疗救助金额 列 如果 医疗救助金额 > 0 则 剔除掉;
    - 导出：导出 应补应退明细表；


3、增加 下放通知 节点页面，由管理员导入；
    - 导入数据模板参考：导出应补应退的那些列，也可能存在一个人有多条记录哦；
    - 下放以前是一个一个的下发，现在改成可以镇街批量下放（预估大概30-50个镇街），交互方式建议是可以用户自定选择、全选之类的，可以与我讨论；
    - 镇街人员 回填信息时 增加  联系人、联系方式、备注（镇街人员独立的，与之前匹配数据的备注字段无关）；
    - 镇街人员  列表和导出 均不能看到  进入报销金额 字段；
    - 管理员 可修改回填信息；
    - 管理员 标记 已报销/未报销；
    - 管理员 可填写 备注（管理员独立的，与之前匹配数据的备注字段无关）；
    - 导出 通知明细；


4、未救助明细、应退应补明细 的差异：
    - 应退应补明细 比 未救助明细 多了 医疗救助金额、渝快保报销金额 两个列；
    - 清洗规则有一点差异：参考第2点的 清洗规则调整;


## 总结：
    - 之前一直未搞清楚 应退应补明细的逻辑，现在搞清楚了未救助明细、应退应补明细 的功能几乎一样（本次改版未救助明细后），都是独立的节点、导入数据，差异是在 应退应补多了 医疗救助金额、渝快保报销金额 两个列；应退应补 的清洗规则多了两个；
    - 未救助明细、应退应补明细、下放通知的数据建议分别各用各的数据表；
    - 导出 未救助明细、应退应补明细 之后，还有很复杂的数据需要在其他内网系统去核对，由人工核对之后再 导入至 下放通知 里；
    - 把之前未注销明细业务流程中的：下放镇街 > 镇街接收、通知和账户回填 > 报销标记  这几个流程  转移到了下放通知里；镇街查看到的数据、以及回填、备注、标记等 也是基于 下放镇街 表，可以理解为 未救助明细 和 应退应补明细只是用于剔除大部分规则的数据，剔除后剩下的少量数据再由人工核对处理后，再通过下放通知 及 后续业务流程；
    - 节点：未救助明细、应退应补明细、下放通知，分别各导出对应明细；



## 功能开发规范
    1. 完整分析原有业务逻辑，应退应补明细的功能需求是基于 未救助明细 本次迭代后 几乎差不多；
    2. 对于不理解的需求需要与我确认、讨论后才能编码调整；
    3. 之前已建数据表及migration是不能动的，如有变更或调整需要重新写migration；
    4. UI、UE 风格需保持与现有一致，有疑问的需与我确认；


-------


参保身份管理

1、导入 （卫健委死亡时间） 文件时，未参保原因为：死亡;
2、镇街人员可填报：未参选原因（配置）、缴费金额(配置)、死亡时间、；管理员编辑：备注；
3、导出 对比结果 里的 医疗救助身份 改为 资助参保身份;





**可用于新会话的上下文**

项目：`/Users/macbook/Documents/codes/tools-hyperf-yf`，Hyperf PHP 后端。

需求：管理参保 / 参保台账调整。

现有相关文件：
- `app/Controller/Enroll/LedgerController.php`
- `app/Job/Enroll/SupplementImportJob.php`
- `app/Job/Enroll/EnrollExportJob.php`
- `app/Service/Enroll/EnrollLedgerService.php`
- `app/Model/Enroll/EnrollLedger.php`
- 参保表迁移：`database/migrations/2026_06_04_100001_create_enroll_tables.php`

核心业务口径：
1. 附件6“卫健委死亡时间 / 死亡名单”导入后，未参保原因写为“死亡”。
2. 导出“对比结果”时，把列“医疗救助身份”改为“资助参保身份”，取值改用 `subsidy_identity`。
3. 镇街人员可填报：
   - 是否参保
   - 未参保原因，配置项
   - 缴费金额，配置项
   - 死亡时间
   - 镇街人员备注，镇街独立字段
4. 未参保原因默认配置：
   - 死亡
   - 服刑
   - 服役
   - 拒参
   - 失踪
   - 无法联系
   - 已注销户口
   - 户口迁出
   - 其他
5. 管理员可编辑管理员备注，管理员独立字段。
6. 管理员也具备镇街填报编辑能力，并触发同样字段联动逻辑。
7. 镇街选择“未参保”时，下拉选择未参保原因。
8. 镇街选择“已参保”时，下拉选择缴费金额。
9. 镇街填报缴费金额后，立即重新计算：
   - 参保类别
   - 是否参保
   - 是否符合资助
   - 是否获得资助
   - 资助方式
10. 如果镇街填报缴费金额与附件4《困难人员参保核实表》导入的缴费金额不一致，显示“待核查”。
11. 管理员流程：先导入附件3全量明细，再导入附件4困难人员参保核实表，然后按需下放给镇街核实填报。
12. 附件4、附件5、附件6导入后，如果仍需镇街核实，由管理员再次或继续下放权限给镇街填报。
13. 下放按镇街选择，支持全选、多选。
14. 也支持管理员筛选列表数据后，复选少量人员手动下放。
15. 下放批次系统自动生成，不需要管理员手填。
16. 不设计管理员“确认、退回”操作。
17. 管理员可收回下放，收回后镇街不可继续填报。
18. 镇街提交后，只要未收回，仍允许继续修改。
19. 管理员可查看下放批次及下放记录。

建议新增或确认字段：

`enroll_ledgers` 建议新增：
- `town_is_insured`
- `town_uninsured_reason`
- `town_resident_payment_amount`
- `town_death_time`
- `town_remark`
- `town_submit_status`
- `town_submitted_at`
- `town_last_filled_by`
- `town_last_filled_at`
- `payment_amount_check_status`
- `payment_amount_check_remark`
- `current_review_batch_id`
- `review_status`

管理员备注：
- 如果现有 `manual_remark` 已经是管理员备注，可以继续复用。
- 如果想语义更清楚，可新增 `admin_remark`，但需要兼容旧导出和旧数据。

建议新增表：

`enroll_review_batches` 下放批次表：
- `id`
- `batch_no`
- `year`
- `period`
- `town_names`，JSON，支持多镇街
- `dispatch_mode`：按镇街 / 手动选择 / 筛选结果
- `filter_snapshot`，JSON，记录下放时筛选条件
- `total_count`
- `status`：已下放、已收回
- `created_by`
- `created_by_name`
- `dispatched_at`
- `recalled_at`
- `remark`
- `created_at`
- `updated_at`

`enroll_review_items` 下放明细表：
- `id`
- `batch_id`
- `ledger_id`
- `town_name`
- `status`：待填报、已保存、已提交、已收回
- `saved_at`
- `submitted_at`
- `recalled_at`
- `created_at`
- `updated_at`

简化状态：
- `review_status`：未下放、待填报、已保存、已提交、已收回
- 不需要：已确认、退回修改

配置项建议：
- 未参保原因写入 `business_filter_options`，`module=enroll`，`type=uninsured_reason`。
- 缴费金额优先从现有参保配置金额中去重生成下拉，例如 `EnrollConfig.personal_amount` 和 `EnrollIdentityAmountConfig.paid_amount`；如后续要人工维护，也可扩展为 `business_filter_options` 的 `resident_payment_amount` 类型。

计算建议：
- 附件4导入金额继续存 `resident_payment_amount`。
- 镇街填报金额存 `town_resident_payment_amount`。
- 有镇街填报金额时，联动计算优先使用镇街金额。
- 如果镇街金额和附件4金额都存在且不一致，`payment_amount_check_status = 待核查`，相关展示结果显示“待核查”。
- 不直接覆盖附件4导入金额，避免丢失系统来源数据。

注意：
- 之前有一版草稿代码已经动过 `LedgerController.php`、`SupplementImportJob.php`、`EnrollExportJob.php`，但不是最终方案；正式实现前应先按这版方案重新整理已有草稿改动。