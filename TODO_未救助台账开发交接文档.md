# 未救助明细台账 - 开发进度交接文档

> **文档说明**：本文件记录了我们针对【未救助明细台账】新模块的最新讨论成果和开发进度。用于时间长了以后，您和 AI 助手能够快速恢复上下文并继续开发。

---

## 🎯 给 AI 的“启动暗号” (下次直接复制这段话发给我)

下次您开启新会话时，**请直接复制下面引用框里的这段话发给我**，我就会瞬间回忆起所有进度并进入工作状态：

> “你好！请阅读项目根目录下的 `TODO_未救助台账开发交接文档.md`，并在 `docs/未救助明细台账_md` 目录下阅读《业务逻辑与流程图.md》和《数据库表设计规范.md》。我们需要接着之前的进度，继续开发【未救助明细台账】的后端服务，现在请开始执行第二阶段：创建 Hyperf 模型和核心 Controller。”

---

## ✅ 我们已经完成了什么？ (已达成进度)

1. **业务逻辑彻底盘清**
   - 梳理清楚了 附件1 与 附件2 的拼接规则。
   - 明确了 附件1 自带 `病种编码` 等字段，将原有的“应补应退台账”数据源统一。
   - 输出并绘制了完整的 Markdown 版业务扭转流转图（存放在 `docs/未救助明细台账_md/业务逻辑与流程图.md`）。
2. **全局底层架构大升级**
   - 抛弃了原来直接给用户表挂字符串的简陋设计。
   - 新增了 SaaS 级别的多租户架构，引入了全局独立的 `towns` (镇街组织架构表)。
   - 在 `users` 表里追加了 `town_id` 用于底层的纵向数据权限隔离。
3. **数据库 Migration 迁移脚本编写完毕**
   - `create_towns_table` (独立的镇街系统表)
   - `add_town_id_to_users_table` (用户表强关联)
   - `create_unrescued_records_table` (未救助明细主表，已加独立镇街查询索引)
   - `create_unrescued_wash_config_table` (动态 JSON 清洗规则表)
   - `create_unrescued_disease_configs_table` (疾病编码对照表)
   - *（所有建表规范和字典详见 `docs/未救助明细台账_md/数据库表设计规范.md`）*

---

## 🚀 接下来该做什么？ (待办任务 TODO)

下次我们直接按照以下清单继续编写代码：

- [ ] **运行建表**：在 Docker 容器内执行 `php bin/hyperf.php migrate`，完成真实的物理表创建。
- [ ] **创建模型 (Models)**：在 `app/Model/Unrescued` 目录下，为刚才的 5 张表分别创建模型类，配置好表关联关系（例如 User 和 Town 的 `belongsTo`）。
- [ ] **编写服务层 (Services)**：在 `app/Service/Unrescued` 创建业务类：
  - 处理 Excel 附件 1、2 的解析、计算与入库更新。
  - 编写“清洗规则”引擎，读取 JSON 进行剔除打标。
- [ ] **编写控制器 (Controllers)**：在 `app/Controller/Unrescued` 目录创建供前端调用的 RESTful API 接口。
- [ ] **编写权限与路由**：接入现有的 JWT 权限系统并注册对应路由。
